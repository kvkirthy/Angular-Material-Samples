Read Me

Prerequisites

NPM need to be installed on the machine.
npm install -g jspm (if not installed already)
npm install -g serve (You may use any simple webserver.)
jspm install


Run the app

(assuming you chose to use serve)
serve Open the app in the browser at default port 3000 http://localhost:3000